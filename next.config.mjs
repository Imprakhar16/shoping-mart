/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['storage.googleapis.com'], // Replace with the actual domain your images come from
    },
  };
  
  export default nextConfig;
  